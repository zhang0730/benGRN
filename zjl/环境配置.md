作者建议：

Ok, which version of scPRINT are you using for bench_perturbseq_genie3_transp.ipynb? is it tagged version 1.6.4? this is the one you should be using.

I see that there, the pyproject.toml unfortunately lists

```
scDataLoader>=1.6.4
GRnnData>=1.1.4
BenGRN>=1.2.4
```

That means if your package manager is set to getting the latest version of packages it might include breaking changes stuff as well.

Can you try to either set your package manager to use the earlier possible version? Else no --no-deps and then pin the minimum versions listed in the https://github.com/cantinilab/scPRINT/blob/1.6.4/pyproject.toml file. e.g.

```
scDataLoader==1.6.4
GRnnData==1.1.4
BenGRN==1.2.4
...
```

you can put all these requirements in a file and do something like so:

```
pip install mypackage --no-deps
pip install -r requirements-min.txt
```

Does that make sense, let me know how it goes



我的思考：

我看到了我的cai026里面的scprint的版本确实是作者说的1.6.4

根据"/disk1/cai026/scprint/scPRINT-main/requirements.txt"里面的

bionty==0.42.6

lamindb==0.69.9
lamindb_setup==0.69.0

scDataLoader==1.6.4
GRnnData==1.1.4
BenGRN==1.2.4

这可能就是标准答案？

```
(benGRN) cai026@ubuntu:~$ pip install scDataLoader==1.6.4 GRnnData==1.1.4 BenGRN==1.2.4

Successfully installed BenGRN-1.2.4 GRnnData-1.1.4 bionty-0.51.2 gotrue-2.8.1 h11-0.14.0 httpcore-0.17.3 httpx-0.24.1 lamin_cli-0.18.0 lamin_utils-0.13.6 lamindb-0.76.12 lamindb_setup-0.78.0 lnschema_core-0.75.0 postgrest-0.13.2 realtime-1.0.6 scDataLoader-1.6.4 storage3-0.7.7 supabase-2.2.1 supafunc-0.3.3 universal_pathlib-0.2.5 websockets-12.0
```

